# Enable the OptimoGo wazo-calld plugin that makes call recordings dual-channel
# (stereo: rx=left, tx=right). xivo's config ChainMap deep-merges this drop-in, so
# the stock calld plugins stay enabled.
enabled_plugins:
  optimogo_stereo_record: true
